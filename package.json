{
  "name": "selenium-test-runner",
  "version": "1.0.0",
  "description": "Command line tool that encapsulates selenium-side-runner and adds extra functionality to it in order to easily update .side files when asserts fail.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "patch-package"
  },
  "author": "Sjoerd van der Hoorn",
  "license": "ISC",
  "dependencies": {
    "patch-package": "^6.2.0",
    "selenium-side-runner": "^3.16.0"
  }
}
